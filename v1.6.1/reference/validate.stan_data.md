# Validate a `stan_data` object

Validate a `stan_data` object

## Usage

``` r
# S3 method for class 'stan_data'
validate(x, ...)
```

## Arguments

- x:

  A `stan_data` object.

- ...:

  Not used.
