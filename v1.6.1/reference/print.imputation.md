# Print `imputation` object

Print `imputation` object

## Usage

``` r
# S3 method for class 'imputation'
print(x, ...)
```

## Arguments

- x:

  An `imputation` object generated by
  [`impute()`](https://openpharma.github.io/rbmi/reference/impute.md).

- ...:

  Not used.
