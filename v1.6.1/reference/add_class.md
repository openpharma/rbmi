# Add a class

Utility function to add a class to an object. Adds the new class after
any existing classes.

## Usage

``` r
add_class(x, cls)
```

## Arguments

- x:

  object to add a class to.

- cls:

  the class to be added.

## Value

`x` with `cls` appended after its existing class attribute.
