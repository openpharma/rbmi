# Validate `sample_single` object

Validate `sample_single` object

## Usage

``` r
# S3 method for class 'sample_single'
validate(x, ...)
```

## Arguments

- x:

  A `sample_single` object generated by
  [`sample_single()`](https://openpharma.github.io/rbmi/reference/sample_single.md).

- ...:

  Not used.
