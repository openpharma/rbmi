# Convert character variables to factor

Provided a vector of variable names this function converts any character
variables into factors. Has no affect on numeric or existing factor
variables

## Usage

``` r
char2fct(data, vars = NULL)
```

## Arguments

- data:

  A data.frame

- vars:

  a character vector of variables in `data`
