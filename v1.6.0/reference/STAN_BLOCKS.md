# List of Stan Blocks

A list with 1 element per standard Stan program blocks. This object is
mostly used internally as a reference for what blocks are parsed from a
covariance / prior Stan definition file.

## Usage

``` r
STAN_BLOCKS
```

## Format

An object of class `list` of length 6.
