# Validate `sample_list` object

Validate `sample_list` object

## Usage

``` r
# S3 method for class 'sample_list'
validate(x, ...)
```

## Arguments

- x:

  A `sample_list` object generated by
  [`sample_list()`](https://openpharma.github.io/rbmi/reference/sample_list.md).

- ...:

  Not used.
