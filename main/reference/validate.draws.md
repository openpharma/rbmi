# Validate `draws` object

Validate `draws` object

## Usage

``` r
# S3 method for class 'draws'
validate(x, ...)
```

## Arguments

- x:

  A `draws` object generated by
  [`as_draws()`](https://insightsengineering.github.io/rbmi/reference/as_draws.md).

- ...:

  Not used.
