# Print `draws` object

Print `draws` object

## Usage

``` r
# S3 method for class 'draws'
print(x, ...)
```

## Arguments

- x:

  A `draws` object generated by
  [`draws()`](https://insightsengineering.github.io/rbmi/reference/draws.md).

- ...:

  not used.
