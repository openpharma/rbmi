% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mmrm_analyse.R
\name{mmrm_analyse}
\alias{mmrm_analyse}
\title{MMRM Analysis Function for Imputed Datasets}
\usage{
mmrm_analyse(
  data,
  vars,
  visits = NULL,
  cov_struct = c("us", "ad", "adh", "ar1", "ar1h", "cs", "csh", "toep", "toeph"),
  weights = c("proportional", "equal"),
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} as provided by \code{\link[=analyse]{analyse()}}.}

\item{vars}{A \code{vars} object as generated by \code{\link[=set_vars]{set_vars()}}. The \code{subjid},
\code{group}, \code{visit}, \code{outcome}, and \code{covariates} elements are used.}

\item{visits}{An optional character vector of visits to extract results for.
If \code{NULL} all visits in \code{data[[vars$visit]]} are used.}

\item{cov_struct}{Character. Covariance structure passed to \code{\link[mmrm:mmrm]{mmrm::mmrm()}}.
Must be one of \code{"us"} (default), \code{"ad"}, \code{"adh"}, \code{"ar1"}, \code{"ar1h"},
\code{"cs"}, \code{"csh"}, \code{"toep"}, or \code{"toeph"}.}

\item{weights}{Character. Weighting strategy for least square means passed to
\code{\link[emmeans:emmeans]{emmeans::emmeans()}}. Either \code{"proportional"} (default, equivalent to
counterfactual/g-computation) or \code{"equal"}.}

\item{...}{Additional arguments passed to \code{\link[mmrm:mmrm]{mmrm::mmrm()}}, in particular
\code{method} and \code{vcov} to control degrees of freedom and variance-covariance
adjustments, and \code{reml} to switch between REML and ML estimation.}
}
\description{
Fits a Mixed Model for Repeated Measures (MMRM) to an imputed dataset and
returns per-visit treatment contrasts, least square means, and residual
variance estimates in the format required by \code{\link[=analyse]{analyse()}}.
}
\details{
A single MMRM is fit across all visits using \code{\link[mmrm:mmrm]{mmrm::mmrm()}}. The model
formula is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{outcome ~ group + visit + covariates + cov_struct(visit | subjid)
}\if{html}{\out{</div>}}

Note that the \code{group:visit} interaction is \strong{not} included by default.
Add it explicitly via \code{vars$covariates} if required, e.g.
\code{set_vars(covariates = c("group*visit"))}.

Per-visit marginal means and treatment contrasts are extracted via
\code{\link[emmeans:emmeans]{emmeans::emmeans()}} using Satterthwaite degrees of freedom.

The return format uses arm-labelled names for all groups:

\if{html}{\out{<div class="sourceCode">}}\preformatted{list(
    var_B_visit_1  = list(est = ..., se = ..., df = ...),
    trt_B_visit_1  = list(est = ..., se = ..., df = ...),
    lsm_A_visit_1  = list(est = ..., se = ..., df = ...),
    lsm_B_visit_1  = list(est = ..., se = ..., df = ...),
    ...
)
}\if{html}{\out{</div>}}

where \code{A} is the reference (first factor level of \code{vars$group}) and \code{B} is
the treatment arm. For 3+ arms, one \verb{var_*} and \verb{trt_*} entry is produced
per non-reference arm.

\verb{var_<arm>_<visit>} is the diagonal element of the MMRM residual covariance
matrix at that visit (i.e. the marginal residual variance). Its \code{se} and
\code{df} are taken from the corresponding treatment contrast.
}
\seealso{
\code{\link[=analyse]{analyse()}}, \code{\link[=ancova]{ancova()}}, \code{\link[mmrm:mmrm]{mmrm::mmrm()}}
}
