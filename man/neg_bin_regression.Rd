% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neg_bin_regression.R
\name{neg_bin_regression}
\alias{neg_bin_regression}
\title{Negative Binomial Regression for Count Outcomes}
\usage{
neg_bin_regression(data, vars)
}
\arguments{
\item{data}{A completed count-outcome \code{data.frame}, normally supplied by
\code{\link[=analyse]{analyse()}}. It must contain one row per subject and period.}

\item{vars}{A \code{vars} object created by \code{\link[=set_vars]{set_vars()}}. The \code{subjid}, \code{outcome},
\code{group}, \code{period}, and \code{duration} elements are required. Any variables in
\code{covariates} must be constant within subject.}
}
\value{
A named list with one element for every non-reference treatment
group. Each element contains the estimated log rate ratio (\code{est}), its
standard error (\code{se}), and infinite degrees of freedom (\code{df}) for
asymptotic normal inference.
}
\description{
Fits a negative binomial regression model to a completed count-outcome
dataset. The period-specific counts and durations are first summed within
each subject. The model is then fitted to the total count with the logarithm
of total duration as an offset.
}
\details{
Treatment effects are returned on the log rate ratio scale so that they can
be pooled with Rubin's rules by \code{\link[=pool]{pool()}}. The first level of \code{vars$group} is
used as the reference group.

The model is
\verb{total count ~ treatment group + covariates + offset(log(total duration))}.
It is fitted by maximum likelihood using \code{\link[MASS:glm.nb]{MASS::glm.nb()}}. The covariance
matrix jointly accounts for estimation of the regression coefficients and
the negative binomial dispersion parameter, matching the calculation used
by SAS \verb{PROC GENMOD}.

The outcome column keeps the name supplied in \code{vars$outcome}; imputation
replaces its missing values in place. \code{neg_bin_regression()} performs the
subject-level aggregation internally, so a separate \code{Imputed_Count} column
is neither created nor required.
}
\seealso{
\code{\link[=analyse]{analyse()}}, \code{\link[=pool]{pool()}}, \code{\link[=set_vars]{set_vars()}}, \code{\link[MASS:glm.nb]{MASS::glm.nb()}}
}
